local:
  hosts:
    localhost:
  vars:
    mconf: Documents/develop/mconf
    backup_location: files
    private_backup_location: private_files
    # More Options
    git_email: ajay39in@gmail.com
    git_name: Ajay Tripathi
    keybase_url: https://prerelease.keybase.io/keybase_amd64.deb
    username: atb00ker
    become: yes
    # Ansible Options
    ansible_python_interpreter: /usr/bin/python3
    ansible_connection: local
